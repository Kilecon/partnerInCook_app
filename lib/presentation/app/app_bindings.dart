import 'package:get/get.dart';

// Minimal AppBindings to satisfy initialBinding reference in main.dart.
// Add global dependencies here when needed.
class AppBindings extends Bindings {
  @override
  void dependencies() {
    // no global dependencies for now
  }
}
